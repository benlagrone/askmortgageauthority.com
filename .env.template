# Copy to .env on the server and fill real values.
WORDPRESS_DB_HOST=host.docker.internal
WORDPRESS_DB_USER=changeme
WORDPRESS_DB_PASSWORD=changeme
WORDPRESS_DB_NAME=wordpress
WORDPRESS_TABLE_PREFIX=wp_
WORDPRESS_DEBUG=false
# Optional URLs/config defines (use \n to separate multiple defines)
# WORDPRESS_CONFIG_EXTRA=define('WP_HOME','https://askmortgageauthority.com');\ndefine('WP_SITEURL','https://askmortgageauthority.com');

# Optional salts/keys (recommended to set):
# WORDPRESS_AUTH_KEY=changeme
# WORDPRESS_SECURE_AUTH_KEY=changeme
# WORDPRESS_LOGGED_IN_KEY=changeme
# WORDPRESS_NONCE_KEY=changeme
# WORDPRESS_AUTH_SALT=changeme
# WORDPRESS_SECURE_AUTH_SALT=changeme
# WORDPRESS_LOGGED_IN_SALT=changeme
# WORDPRESS_NONCE_SALT=changeme

# If using bundled MariaDB profile (not typical when keeping existing DB):
# MARIADB_ROOT_PASSWORD=changeme
